
#pragma once

#include <string>
#include <iostream>

using std::string;

// Enum representing degree programs shown in rubric, Task C
enum DegreeProgram { SECURITY, NETWORK, SOFTWARE };

// String Array showing the same degree programs, for use in later functions
const string degreeProgramStrings[] = {"SECURITY", "NETWORK", "SOFTWARE"};
